export default function TagHabilidad({ label }) {
  return (
    <span className="text-[9px] font-medium px-2 py-0.5 rounded"
      style={{ background: '#f0f2f5', color: '#6b7a8d' }}>
      {label}
    </span>
  )
}